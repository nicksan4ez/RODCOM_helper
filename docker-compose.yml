services:
  rodcom-bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - rodcom-data:/data
    mem_limit: 128m
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  rodcom-data:
